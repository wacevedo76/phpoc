__pycache__/
*.pyc
*.pyo

# Operational docs (root level only)
/_Operational*

# Trace logs — may contain sensitive data
staging_log/

# Node.js dependencies (worker project)
node_modules/

# Pi agent instructions — per-user preferences, not project code
AGENTS.md

# Build artifacts
phpoc-web/dist/
