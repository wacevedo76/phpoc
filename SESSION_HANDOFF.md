# PH Ledger — Session Handoff

> **Agent:** On first read, run `git log --oneline -5 && echo "---changed-files---" && git diff --stat HEAD~2` to load recent context.
> Before making edits, consult the Documentation Impact Contract in root `AGENTS.md`.
> For architectural decisions, read `docs/design/TOP_LEVEL_DIRECTIVES.md` first — D1–D10 are the binding principles.
>
> **⚠️ Git operations require user approval.** Never run `git commit` or `git push` automatically. Ask first.
>
> **Full issue queue:** `docs/planning/BACKLOG.md`
> **Completed milestones (archived):** `docs/planning/archive/` (…2026-08-21, 2026-08-22, 2026-08-28, **2026-08-29**)

## Current State
- **Branch:** `Flutter-features_and_ux` → pushed `d3d6e68` (C-2 seed re-key `ph rekey-seed` + transport adapter gap + Flutter content_hash parity)
- **C-2 CLI↔client cross-client verify — 4-phase TDD COMPLETE (2026-08-29):** `docs/planning/C2_CLI_CLIENT_VERIFY_PHASE1.md` — option (a) raw-seed re-key (ADR-032); hermetic matrix GREEN all 4 directions; Phase 4 (REFACTOR + Group E docs) DONE. Detail archived 2026-08-29.
- **C-2 Live R2 E2E ✅ GREEN (2026-08-29):** restored canonical test ledger to R2 (genesis `e718daf3…`, 31 blocks, hash-index consistent) + fixed forward leg (`renew_seed()` `transport=None` → isolated push, no canonical `ledger/blocks/` overwrite). `test_c2_cli_client_live_r2.py` 2/2 + `test_c2_live_r2.py` 1/1.
- **Flutter test suite:** `+2147` / 0 failures (4 skip) · **Remote sync E2E:** 8/8 GREEN (`--timeout 180s`)
- **Phone `RFCW50FZQPJ`:** debug 0.1.0 deployed + local ledger repaired (132 blocks == remote, 280 staging rows)

## Immediate Next Steps 🎯
- **🎯 Cross-client staging convergence (Web ↔ Flutter ↔ CLI) — ACTIVE:** `docs/planning/CROSS_CLIENT_STAGING_CONVERGENCE_PLAN.md`. Sequence: **C1** Web restore stops skipping `active` rows (client-local, aligns to CLI) → **C2** Web periodic+focus pull → **C3** read-only observe mode (protocol, CLI-first) → **C4** cookie `seq` + Worker CAS (last). CLI = reference anchor (I2/I3). **C1 Phase 3 (GREEN) DONE (2026-09-07):** `rowsFromRemoteBlob` exported from `sync.js` (+ flat-web-row `synthesizeActivityBlob` branch) + `connectToWorker` ad-hoc active-skip loop replaced with `rowsFromRemoteBlob → mergeRows([],rows) → canonicalRowToDTO → committed-filter`. Fixed a Phase 2 mirror bug (passed raw JSON string, not parsed object). GREEN: `worker_connect_active_rows_test.mjs` 50/50 + `worker_connect_fullchain_regression_test.mjs` 23/23, no regressions (sync_service 316, cross_client 78, ccs2 41, etc.), vitest 181/1skip, vite build ✅. **C1 Phase 4 (REFACTOR) DONE (2026-09-07):** flat rows in `rowsFromRemoteBlob` now reuse the shared `dtoToCanonicalRow` converter (removed hand-rolled `synthesizeActivityBlob`), so flat `activity_status` derives from `is_active`/`is_paused` (was: defaulted to `active`). `worker_connect_active_rows_test.mjs` 54/54 (+4 flat paused/ended guards) + `worker_connect_fullchain_regression_test.mjs` 23/23; regressions all GREEN (sync_service 316, row_sync 108, cross_client 78, ccs2 41, staging_alignment 24, etc.), vitest 181/1skip, vite build ✅. **C1 COMPLETE. C2 (Web foreground/focus pull) — Phase 1 (test exploration) DONE (2026-09-09):** `docs/planning/C2_WEB_FOCUS_PULL_PHASE1.md` (25 assertions, groups A–F) — event-driven `checkAndSync()` pull on `visibilitychange`/`focus`/`pageshow`/mount via new `useFocusPull`/`createFocusPull` hook (`src/hooks/useFocusPull.js`); reuses the existing sync gate unchanged (client-local, no contract change; ADR-034 DS4 event-driven, not periodic). **✅ C2 Phase 2 (RED) DONE (2026-09-09):** `phpoc-web/test/focus_pull_hook_test.mjs` written — 25 tests (Groups A–F), 25/25 RED against the missing `src/hooks/useFocusPull.js`. **✅ C2 Phase 3 (GREEN) DONE (2026-09-09):** `phpoc-web/src/hooks/useFocusPull.js` implemented (`createFocusPull` pure factory + `useFocusPull` React wrapper) — event-driven `checkAndSync()` pull on `visibilitychange`/`focus`/`pageshow`/mount, single-flight + coalesced `_pending` re-run, `onResult` surfaces the SyncResult without auto-claiming (I1), rejecting sync swallowed via `console.warn`. GREEN: `node test/focus_pull_hook_test.mjs` 25/25 (49 assertions), exit 0. **✅ C2 Phase 4 (REFACTOR) DONE (2026-09-09):** added the missing React import (`useRef`/`useEffect`/`useCallback`), made the wrapper's `isSyncing` stable via `useCallback` (mirrors `useAutoSync`), DRY'd listener add/remove into `_bindEvents`. GREEN: `focus_pull_hook_test.mjs` 49/49 + `auto_sync_hook_test.mjs` 63/63, `node --check` clean. **C2 COMPLETE. → C3 (read-only observe mode, protocol CLI-first) — Phase 1 (test exploration) DONE (2026-09-09):** `docs/planning/C3_READ_ONLY_OBSERVE_PHASE1.md` (41 assertions, groups A–G) — OBSERVE branch to §12 (pull cookie → `staging_hash` change gate → blob + `merge_rows` → `READY`, no push / no cookie claim / no TTL refresh), shared pull+merge helper extracted from `_reconcile_and_claim` (`service.py:897`), CLI `view`/`list`/`tags` wired to observe (writes stay `require_auth=True`), offline-lenient (D6), I7 reconciled via `staging_hash`; Web/Flutter parity ports (Group E) + PHPSPEC/§12 docs (Group F) blueprinted future. Resolved: D-C3-1 (config `staging.observe_mode` `auto`|`manual` + `--observe` flag), R-C3-1 (injected `staging_hash_provider` seam, full-pull stepping stone), A-C3-2 (converge on canonical `merge_rows`, B6 re-scoped). **✅ C3 Phase 2 (RED) DONE (2026-09):** `tests/test_staging_observe.py` — 31 tests (A=8/B=7/C=6/D=5/G=5) all RED for the right reasons (missing `observe()`/`_pull_and_merge()`, `CLIInterface(config=…)` kwarg, `ConfigManager.DEFAULTS["staging"]["observe_mode"]`; B6/B7 expose the legacy-merge remote-wins vs canonical LWW + ADR-033 terminal-state divergence). **Phase 3 must also update `tests/test_cli_interface.py` (~10 tests) as observe defaults to `auto`:** re-scope `A2`/`A3` + `B1–B7` (pin `check_and_sync` on the default read path) to `observe=False` (manual fast-path regression); re-home `B7`'s REAUTH "showing local data" notification to the `observe=False` path only; fix F2-cache `test_B3_cache_hit_list_habits_includes_cached_entries` (relies on `check_and_sync → READY → _sync_remote_ledger_and_dedup`; decide: observe does no ledger pull → assert local-only synced view, or set `observe=False`); keep read methods calling `_sync_before_command(require_auth=False)` (no explicit `observe=`) so `D1`/`D2` `assert_called_once_with(require_auth=False)` stay green; make `observe=None` + `config=None` default to `auto`. **✅ C3 Phase 3 (GREEN) DONE (2026-09):** `domain/staging/service.py` gained `observe()` (fail-open READY: no-remote → cookie pull → injected-hash gate → `_pull_and_merge` → `_write_last_seen_hash` only after a successful merge; never REAUTH_NEEDED, never push blob, never claim/push cookie, never refresh TTL), `_pull_and_merge()` (blob pull + canonical `_merge_remote_into_local`, returns None on `BLOB_KEY_MISMATCH` untouched-local, propagates `TimeoutError`), and `_read_last_seen_hash()`/`_write_last_seen_hash()` (META_FILE, preserves `device_specifier`/`creation_time`); `_reconcile_and_claim` refactored to call `_pull_and_merge`; fixed `_merge_remote_into_local` `remote_won_ids` to use `MergeEngine._remote_wins` (terminal-state LWW, B7). `security/config_manager.py` `DEFAULTS["staging"]["observe_mode"]="auto"`; `phpoc_cli/interface.py` `CLIInterface(config=…)` + `_should_observe()` routing (reads auto-observe via `observe()`, writes stay `check_and_sync`); `main.py` passes `config=CONFIG` to all 7 `CLIInterface` constructions; re-scoped `test_cli_interface.py` A2/A3+B1–B7 + `test_p4_cli_ux_polish.py` E1/E4 to `observe=False` and `_BaseCacheIntegration` to a `"manual"` mock config. GREEN: `test_staging_observe.py` (31) + `test_cli_interface.py` (78) + `test_p4_cli_ux_polish.py` (24) + `test_staging_service.py` + `test_staging_sync_optimization.py` + `test_cli_sync_gate_wiring.py` = 273 passed / 0 fail. (Pre-existing unrelated: `test_staging_hash_parity.py` 5 RED — P0 `compute_staging_hash` + `comment ""→null` coercion, out of C3 scope.) **✅ C3 Phase 4 (REFACTOR) DONE (2026-09):** code review + 2 fixes in `service.py` — (1) dead code removed from `_reconcile_and_claim` (unused `remote_device_uuid`/`remote_cookie_specifier`/`local_device_uuid` locals + `DeviceCookie.parse_remote` block; `pull_cookie` reduced to reachability probe per Bug-3a always-pull+merge); (2) `_touch_local_cookie` now preserves existing META_FILE keys (esp. `last_seen_hash`) instead of rewriting to `{device_specifier, creation_time}` — stops observe's cheap-read baseline being lost on local writes. Regression `test_A9_local_write_preserves_last_seen_hash` (32/32). **Group F docs closed:** §12.3.1 OBSERVE branch + §12.4 OBSERVE rows + I7 re-worded (`CROSS_CLIENT_STAGE_SYNCING_REFERENCE.md`), PHPSPEC §8.10 (device cookie + observe mode + observe→claim I1), `CLI_READONLY_STAGING_SYNC.md` generalized. **C3 COMPLETE → C4 = ADR-034 P0 (`staging_hash` + `seq`/CAS) — P0 parity gate COMPLETE (2026-09).**
- **🟡 ADR-034 `staging_hash` + `seq`/CAS cookie migration (= convergence C4, merged) — P0 parity gate, 4-phase TDD COMPLETE (2026-09):** `testdata/staging_hash_seed.json` (4-row seed + golden `staging_hash` `dfe14053…` + golden `canonical_array`, digest verified by hand re-derivation). **Phase 1 (test exploration) DONE:** `docs/planning/STAGING_HASH_PARITY_PHASE1.md` (22 assertions, groups A–G, V1/V2 parity gate; key findings: Flutter has no `dtoToCanonicalRow` and its `_buildActivityData` key order is non-canonical → needs canonical builder + `computeStagingHash` in new `lib/data/sync/staging_hash.dart`; use `encodeValueNoSpaces` NOT `jsonSort`). **✅ Phase 2 (RED) DONE (2026-09):** `tests/test_staging_hash_parity.py` expanded → Groups A (A1–A5) + B (B1–B3) + G1 (Python↔Web, drives `node phpoc-web/test/staging_hash_parity.mjs` via stdin/stdout); `phpoc-web/test/staging_hash_parity.mjs` (hybrid: `node --test` C1–C3/D1–D2 self-tests + subprocess ops `canonicalRows`/`canonicalArray`/`stagingHash`); `phpoc-flutter/test/data/sync/staging_hash_test.dart` (E1–E4/F1–F2, compile-RED on missing `lib/data/sync/staging_hash.dart`). **RED verified: Python 8 fail / 3 green / 1 skip (V1 `comment ''→null` + V2 `compute_staging_hash`/`computeStagingHash` missing); Web 3 pass / 2 fail (V1 guard-green, V2 RED); Flutter compile-fail.** **✅ Phase 3 (GREEN) DONE (2026-09):** implemented `compute_staging_hash` (Python `row_merge.py` — `comment` ''→null + canonical-array SHA-256 lowercase hex), `computeStagingHash` (Web `remote_sync.js` — pure synchronous self-contained SHA-256 + `jsonSortNoSpaces`), and new `lib/data/sync/staging_hash.dart` (Flutter pinned-key-order `dtoToCanonicalRow` + `computeStagingHash` via `encodeValueNoSpaces`). GREEN all three legs: Python 11 pass/1 skip (`test_staging_hash_parity.py`) · Web 5/5 (`node --test phpoc-web/test/staging_hash_parity.mjs`) · Flutter 6/6 (`flutter test test/data/sync/staging_hash_test.dart`); no committed-test edits; regressions clean (Python 2741/2skip · Web vitest 181/1skip · `flutter analyze` 0). **✅ Phase 4 (REFACTOR) DONE (2026-09):** extracted the inlined ~120-line SHA-256 out of `remote_sync.js` into new `phpoc-web/src/crypto/sha256.js` (`sha256Hex`) — remote-sync module stays transport-focused, digest helper is reusable + independently `node:crypto`-cross-checked (D2); fixed the stale "temporary / Phase 2 replaces" note in `core/utils/hash_utils.dart` (now documents the staging-hash gate dependency). Regression: Python 11/1skip · Web `node --test` 5/5 + vitest 181/1skip · Flutter 6/6 + `dart analyze` clean. **P0 parity gate COMPLETE. → P1 cookie schema + Worker CAS (`seq` + stale-write guard, §3a) — Phase 1 (test exploration) DONE (2026-09):** `docs/planning/STAGING_COOKIE_CAS_PHASE1.md` (30 assertions: Group E cookie schema ×3 clients, Group J Worker CAS + client `next_seq`). **✅ Phase 2 (RED) DONE (2026-09):** 6 test files — `tests/test_staging_cookie_cas.py` (E1–E6+J11), `phpoc-web/test/staging_cookie_cas.mjs` (E7–E11+J12), `phpoc-flutter/test/data/sync/device_cookie_test.dart` (extended E12–E15+J13), `phpoc-flutter/test/core/models/device_cookie_test.dart` (extended E16/E17), `worker/test/cookie_cas.test.ts` (J1–J5 hermetic, guarded dynamic import), `worker/test/cookie_cas_live.test.ts` (J6–J10 live, deferred to Phase 3 — needs `PHPOC_API_KEY` + deployed Worker). RED verified: Python 4 RED / 3 guard-green (E4–E6) · Web 4 RED / 2 guard (E10/E11) · Flutter data/sync 3 RED / 2 guard (E14/E15) + core/models compile-fail (E16/E17) · Worker J1–J5 5 RED. **✅ Phase 3 (GREEN) DONE (2026-09):** implemented across 6 files — Python `domain/cookie/device_cookie.py` (`create()`/`create_local()` optional `staging_hash`/`seq`/`last_seen_hash`/`last_seen_seq`; `next_seq()` absent→1/N→N+1; `parse_remote()` tolerant), Web `phpoc-web/src/sync/cookie.js` (`create({stagingHash,seq})` + `nextSeq()`), Flutter `lib/data/sync/device_cookie.dart` (`create()` + full-map `isValidLocally()` + `nextSeq()`) + `lib/core/models/device_cookie.dart` (4 new fields, backward-compat `fromJson`), Worker new `src/cookie_cas.ts` (`isStaleWrite(incomingSeq?, storedSeq?)`) + `src/index.ts` (device-cookie PUT CAS → 409 on stale `seq`). GREEN: Python 7/7 · Web 6/6 · Worker hermetic J1–J5 5/5 · Flutter 29/29; full suites GREEN (Python 2748/2skip · Web sync 392 + vitest 181/1skip · Flutter 2199/4skip); `tsc` clean in `src/`. **Live J6–J10 deferred** — needs `npx wrangler deploy -c wrangler.testing.toml` + `PHPOC_API_KEY` (wrangler not logged in). Docs updated: PHPSPEC §8.10 (`seq`/`last_seen_seq` + CAS), `worker/AGENTS.md` (ownership + CAS contract), MAP.md (new files + P1 status). **✅ Phase 4 (REFACTOR) DONE (2026-09):** extracted duplicated incoming/stored `seq` JSON parse from `worker/src/index.ts` into `cookie_cas.ts` `readCookieSeq(body)` (hermetic 9/9: J1–J5 + 4 extraction guards); Python `_write_meta` shared helper + corrected the stale schema docstrings ×3 clients (Python/Web/Flutter); Web TTL math simplified (removed unused `DEFAULT_TTL_MS`). Regression GREEN: Python 2748/2skip · Web sync 76 + sync_service 316 + reauth_ttl 50 + staging_cookie_cas 6 · Worker 9/9 + `tsc` clean in `src/` · Flutter 29/29. **ADR-034 P1 COMPLETE. P2 (mutation flow) — Phase 1 (test exploration) DONE (2026-09-23):** `docs/planning/STAGING_MUTATION_WIRING_PHASE1.md` (28 assertions: Group F mutation flow F1–F24 ×3 clients + Group H specifier-stability I5 H1–H4) — every mutation push recomputes `staging_hash` (canonical non-committed rows) + bumps `seq` (`next_seq(last_seen_seq)`), updates the local cookie (`last_seen_hash`/`last_seen_seq`, bump `creation_time`, keep `device_specifier`), blob-before-cookie (I3/I8), specifier re-rolled only on handoff (I5). Key decisions: hash the exact pushed uncommitted row set (flag Flutter `_rowIsCommitted` vs `computeStagingHash` committed-filter parity), handoff claim writes hash+seq too. Plan `STAGING_CHANGE_DETECTION_PLAN.md` §10 (P0→P5). **✅ Phase 2 (RED) DONE (2026-09-23):** three test files — `tests/test_staging_mutation_wiring.py` (F1–F9 + H1–H2, 9 RED / 2 guard-green F4/F7), `phpoc-web/test/staging_mutation_wiring.mjs` (F10–F17 + H3, 8 RED / 1 guard-green F13), `phpoc-flutter/test/data/sync/staging_mutation_wiring_test.dart` (F18–F24 + H4, 7 RED / 1 guard-green F23). All failures are assertion errors (`staging_hash` null/undefined, `seq` missing/`0`, specifier re-rolled on every push, offline not fail-open); no import/compile errors. Flutter tests drive public APIs only (`pushToRemote()`/`reconcileAndClaim()`) and hash a 5-field canonical projection of the `_rowIsCommitted`-filtered row set. → **Next: P2 Phase 3 (GREEN).**
- **🔜 P1 Commonplace web port — Slice 6 (shared key-rotation extension) — Flutter Phase 1 (test exploration) DONE:** `docs/planning/flutter/COMMONPLACE_BOOK_KEY_ROTATION_PHASE1.md` (**48 assertions, groups A/C/D/E — re-scoped to hard-only**). Scope: Flutter `deriveMk(seed,version)` + **hard-only** rotation (new `KeyRotationService`) **and** Commonplace lockstep re-encrypt. **D-ROT-1 ✅ RESOLVED by ADR-026a (2026-09-07):** `key_version` is out-of-band derivation metadata (v0 raw / v≥1 HMAC), rotation hard-only, `verify()` takes a single MK. **→ Next: Phase 2 (RED) — define the 48 hard-only assertions as tests.**
- **✅ Terminal-state rule — "ended" is permanent (ADR-033) — 4-phase TDD COMPLETE (2026-09-03):** `row_sync.js` (`isEnded`/`effectiveStatus`/`remoteWins` + terminal-state branch in `mergeRows`), `sync.js` `_mergeRemoteIntoLocal` (rebuild via shared `remoteWins`), `merge_engine.py` `merge_rows` (`_is_ended`/`_effective_status`/`_remote_wins`), PHPSPEC §8.5 documented. K/L/M/N all GREEN (24 web / 9 M / 4 N). **Phase 4 (REFACTOR):** named the terminal-state branch explicitly (`terminalEndWins` / `_terminal_end_winner` in `remoteWins`/`_remote_wins`) + deduped the 3× merged-row construction into `buildMergedRow` (mirrors Python `_build`). Regression: full Python suite 2690 pass / 1 skip (3 live deselected), web row_sync 108 / ccs2 41 / cross_client 78 / sync_service 316 / staging_alignment 24 all GREEN.
- **✅ Option A — web staging `updated_at` persistence — 4-phase TDD COMPLETE (2026-09-03):** `docs/planning/WEB_STAGING_UPDATED_AT_PHASE1.md` (14 assertions A–E) — fixes cross_client 2.4c by persisting `updated_at` through the legacy `LocalCache` path (root cause: `_mergeRemoteIntoLocal` backfilled local + legacy-remote to the same `now` → artificial LWW tie → local-wins masked remote `ended`; PHPSPEC §8.1 violated). **Phase 3 (GREEN):** `local_cache.js` (`append`/`update`/`addPause`/`closePause` bump `updated_at`, `_rawToDto` backfills to `start_epoch`, `_dtoToRaw` persists, `markCommitted` no-op, `options.now` seam) + `entry_dto.js` (`canonicalRowToDTO`/`rawEntryToDTO` emit `updated_at`). 13 GREEN + 1 guard (`local_cache_updated_at` 11, `entry_dto_updated_at` 2, `sync_push_updated_at` 2). **Phase 4 (REFACTOR):** fixed 2.4c same-millisecond tie flake (backdated local `updated_at` to `start_epoch` in `cross_client_web_test.mjs`; 5/5 deterministic 78/0) + documented `updated_at` in the `StagingEntry` typedef/storage-format comments. Acceptance: cross_client 78/0, ccs2 A4 41/0, sync_service 316/0. **→ Next: terminal-state rule (ADR-033) — Phase 1 blueprint done.**
- **✅ P1 Commonplace web port — Slice 5 (Remote sync) Phases 1–4 COMPLETE (2026-09-03):** 31 tests / 92 assertions GREEN (`commonplace_{push_service,pull_service,reconcile,sync_e2e}_test.mjs`). Phase 3 (GREEN): `CommonplacePushService`/`CommonplacePullService` + `CommonplaceService.reconcileRemoteChain` + `CommonplaceChain.verifyBlocks`/`reconcileRemoteChain` + `ledger/utils.js` `jsonSortNoSpaces` + `sync/keys.js` `REMOTE_COMMONPLACE_*`. Phase 4 (REFACTOR): extracted `reconcileChainCore` (`src/ledger/chain_reconcile.js`) + shared push/pull/freshness helpers (`src/sync/chain_transport_helpers.js` — `chainBlockPath`/`readRemoteHashIndex`/`pushChainPayloads`/`pullRemoteHasMore`); Slice 1/3 regression re-verified GREEN (chain 67 / engine 28 / storage 19 / ad-hoc 16 / service 37). One Phase 2 assertion corrected (P3 space-separator check). **→ Next: Slice 6 key-rotation extension (Flutter Phase 1 blueprint done — see top).**
- **✅ P1 Commonplace web port — Slice 1 complete (Phases 1–4, 2026-08-31):** `phpoc-web/src/commonplace/{commonplace_chain,commonplace_engine,commonplace_storage}.js` + `seal_fields.js` `commonplace_genesis`/`commonplace` whitelist. 55 tests / 130 assertions GREEN (`node test/commonplace_*_test.mjs`). Phase 4 deduped `parseFormatVersion`/`isFormatVersionAtLeast`/`CONTENT_HASH_REQUIRED_VERSION`/`ZERO_HASH_64`/`computeContentHash` into `ledger/utils.js` (were duplicated across `chain.js`, `engine.js`, `commonplace_chain.js`); ledger/merge/genesis-gate/seal suites re-verified GREEN — 7 pre-existing web failures unchanged, no new regressions. **→ Slice 2 (Book Switcher) ✅ complete 2026-08-31.**
- **✅ P1 Commonplace web port — Slice 2 (Book Switcher) complete (Phases 1–4, 2026-08-31):** `src/commonplace/book.js` (`Book` identity + `getBookMode`/`setBookMode` localStorage `phpoc_book_mode`) + `src/components/layout/BookSwitcher.jsx` rendered above page content in `AppLayout` + `.book-switcher*` styles; 13 tests GREEN (`book_switcher_web.test.mjs`). Blueprint `docs/planning/COMMONPLACE_BOOK_SWITCHER_WEB_PHASE1.md`. **→ Next: Slice 3 (UI wiring).**
- ✅ **P1 Commonplace web port — Slice 3 (UI wiring) complete (Phases 1–4, 2026-08-31):** all 40 tests GREEN — `commonplace_service_test.mjs` (S+V, 37 assertions via `node`) + `commonplace_screen_web.test.mjs` (L+A+T, 19) + `commonplace_swap_web.test.mjs` (R, 6), plus Slice 2 `book_switcher_web.test.mjs` (13) still GREEN. Implemented `commonplace_service.js`, `book_mode.jsx` (`BookModeProvider`/`useBookMode` — provider-less fallback so standalone `BookSwitcher` still works), `CommonplaceScreen`/`AddEntrySheet`/`TopicIndex`, `BookBody` content-swap, `BookSwitcher`→`useBookMode()`, `DevModeContext.services.commonplaceService` + `App.jsx` `BookModeProvider`/`BookBody` + `.commonplace-*` styles. `vite build` ✅. One Phase 2 test assertion corrected (V2 `blocksB.length===0` → `undefined||length===0`, matching S10 + Flutter fresh-chain semantics). **Phase 4 (REFACTOR) ✅** — extracted `usePersistedBookState` (`book_mode.jsx`) + `normalizeTags` (`commonplace_service.js`); full suite re-verified GREEN (vitest 157 passed / 1 skip). **→ Next: Slice 4 (Settings surface).**
- ✅ **P1 Commonplace web port — Slice 4 (Settings surface) Phase 2 (RED) DONE (2026-08-31):** 34 RED tests across 4 files — `test/commonplace_settings_swap_web.test.mjs` (S1–S6: S2/S3/S4/S5 RED, S1/S6 ledger regression-green), `test/commonplace_settings_screen_web.test.mjs` (W/P/V/B4/C/X/R8 = 18 RED via defensive `()=>null` stub — variable-path dynamic import so missing module fails as assertions, not `ERR_MODULE_NOT_FOUND`), `test/commonplace_settings_service_test.mjs` (B1–B3 RED — `exportForBackup`/`restoreFromBackup` not yet on `CommonplaceService`), `test/commonplace_settings_rekey_test.mjs` (R1/R2/R3/R5/R6/R7 RED, R4 ledger regression-green — `RekeyService` ignores `commonplace:blocks`). **31 RED / 3 intentional-green guards.** Run: `node test/commonplace_settings_service_test.mjs`, `node --test test/commonplace_settings_rekey_test.mjs`, `npx vitest run test/commonplace_settings_{swap,screen}_web.test.mjs`. **✅ Phase 3 (GREEN) + Phase 4 (REFACTOR) DONE (2026-08-31):** all 44 test cases GREEN — `commonplace_settings_service_test.mjs` (13), `commonplace_settings_rekey_test.mjs` (7), `commonplace_settings_swap_web.test.mjs` (6), `commonplace_settings_screen_web.test.mjs` (18). Implemented `CommonplaceSettingsScreen` + `BookBody` `settings`/`dashboard`-only swap (over-swap fix) + `CommonplaceService.exportForBackup`/`restoreFromBackup` + `RekeyService` re-encrypts `commonplace:blocks` in lockstep (flattened genesis; R6 abort-before-write). **Phase 4 (REFACTOR):** extracted shared `useRekeyFlow` hook (`src/hooks/useRekeyFlow.js`) + `RekeyModal` component (`src/components/modals/RekeyModal.jsx`) — deduped the re-key modal/state duplicated across ledger `Settings.jsx` and `CommonplaceSettingsScreen.jsx` — and unified `_rebuildBlocks`/`_rebuildCommonplaceBlocks` into one `_rebuildChain` engine in `rekey_service.js`. **→ Next: Slice 5 (Remote sync) — see top entry (Phase 1 done, Phase 2 RED next).**
- ⚠️ **Do NOT run `node --test` over the whole `test/` dir** (observed 71-min hang, 2026-08-31): `c2_live_rekey.mjs`/`c2_cli_rekey_verify.mjs` are stdin-driven Python subprocess helpers (`tests/test_c2_live_r2.py`/`tests/test_c2_cli_client_verify.py`) that block forever without their driver; `c2_fixture_gen.mjs` is a CLI generator. Run Node tests individually (`node test/<name>.mjs`) + `npx vitest run` for `.test.mjs` components.
- **✅ C-2 `identity_pub_key` raw-bytes parity (Web + Flutter) — Phase 3 (GREEN) DONE (2026-08-31):** raw-bytes `identity_pub_key` implemented end-to-end — Rust `digest::identity_pub_key_hex` + `wasm.rs::identity_pub_key` + `frb.rs::identity_pub_key`; WASM rebuilt + copied to `phpoc-web/src/crypto/wasm/*` (also exported `hmac_hex`/`derive_field_key`); Web `CryptoService.identityPubKey` → `identity_pub_key` binding; Flutter `CryptoService`/`CryptoServiceNative.identityPubKey` → `frb_generated.dart::identityPubKey`; call sites switched (`chain.js:289`, `c2_fixture_gen.mjs`, `rekey_service_web_test.mjs`); fixtures regenerated `271a413b…` → `9a2db2e2…`; Python `test_c8_genesis_parity_after_cli_pull` fixed (string-hash → raw-bytes). Blueprint `docs/planning/C2_IDENTITY_PUB_KEY_RAW_BYTES_PHASE1.md` (29 assertions) → Phase 4 (REFACTOR) ✅. **Full-suite re-run GREEN (2026-08-31):** Python `2685 passed / 1 skip / 0 fail`, Flutter `2147 passed / 4 skip / 0 fail`; targeted Rust `cargo test` 65+15, Web crypto suites + `identity_pub_key_web_test.mjs` 8/8 + `c2_cross_client_verify.mjs` 18/2skip + `rekey_service_web_test.mjs` 29/29 + vitest 119/1skip all GREEN.
- **🟠 Web↔Flutter parity queue (2026-08-28, ordered):** spec `docs/planning/WEB_FLUTTER_PARITY_SPEC.md`. P4 ✅ → P2 (C-2 cross-client verify, Web↔Flutter) ✅ → **P1 `COMMONPLACE_BOOK_WEB_ROADMAP.md` (Commonplace web port; slices 1–4 done, 5 now [Flutter sync landed], 6 — Flutter Phase 1 blueprint done (`COMMONPLACE_BOOK_KEY_ROTATION_PHASE1.md`, re-scoped to hard-only per ADR-026a, Phase 2 (RED) next)) → P3 `WEB_STAGING_OPTION_A_PHASE1.md` (staging Option A refactor, last — no user-facing value).**
- **🟡 C-2 Seed Re-Key cross-client — status:** Flutter ✅ (option a, 2026-08-22) · Web ✅ 4-phase TDD (2026-08-24, COMMITTED `4364ac2`) · CLI Phase A ✅ (2026-08-29) · Web↔Flutter verify ✅ (2026-08-28) · CLI↔client verify ✅ (2026-08-29) · raw-bytes parity ✅ (2026-08-31). **C-2 is complete.**

## Known Issues
- **Flutter History "no activities" after cloud restore (FIXED 2026-09-07):** `HistoryScreen` defaulted `_selectedCalendarDate` to today, hiding the 288 committed staging rows restored from R2 behind today's 3 entries (data was intact — `staging` seeded correctly; `entries`/`index_entries` are legacy/unused for display). Fix: default `_selectedCalendarDate` to `null` (full chronological list on open) + `_clearAllFilters()` wired to the empty-state "Clear filter" button (was a no-op `_clearRangeFilter` when only a calendar date was selected). `history_screen_test.dart` 35/35 GREEN (helper `_clearCalendarFilter` made robust — now targets the delete tooltip, no-op when no filter). **Web parity note:** phpoc-web `History.jsx` still defaults `filterDate` to today — divergence flagged for user decision.
- **`scripts/verify_ledger.py` content-hash keep-`_enc`-suffix gap (FIXED 2026-09-06):** `verify_content_hash` only tried strip-`_enc` forms, so entries written post-2026-08-28 (Flutter keep-suffix canonical, PHPSPEC §5.5/§6.1) reported a false `INVALID` — reproduced on personal-ledger snapshot `~/code/phpoc-testing-data/2026-09-04-ledger.json` block 141 entry 0. Added the canonical keep-suffix candidate (order keep-compact → strip-compact → strip-indent2) + `keep_enc_suffix` kwarg on `_build_canonical_map`; ledger now verifies `VALID (144 blocks)` with the personal seed. Unrelated pre-existing: `testdata/ledger.json` day seals under `block_hash` vs expected `day_hash`.
- **Web suite — 45 pre-existing failures remediated 2026-09-03 (fixture-only, items #1/#3–#7):** `settings_genesis` 13/0, `import_entries` 55/0, `ledger_sync` 76/0, `reauth_genesis_mismatch` 47/0, `sync_service` 316/0 (fixtures now seal via `selectSealFields` + `computeEntryHash`/`computeContentHash` + date-based mismatch on the sealed `date` field). **Remaining (out of fixture scope):** (a) **8 unlock cookie failures** (item #2 — `REAUTH_NEEDED` creates no local cookie; TDD RED not-yet-implemented; excluded by user decision). **(b) cross_client 2.4c ✅ RESOLVED (Option A, 4-phase TDD complete 2026-09-03):** `LocalCache` now persists `updated_at` (was: `_mergeRemoteIntoLocal` backfilled local + legacy-remote to the same `now` → artificial LWW tie → §8.5 local-wins masked remote `ended`); deterministic 78/0 (Phase 4 fixed a same-millisecond tie flake). Terminal-state rule **IMPLEMENTED (ADR-033, 4-phase TDD complete 2026-09-03):** "ended" is permanent — ported Flutter's rule to Web + Python + §8.5 (blueprint `TERMINAL_STATE_END_RULE_PHASE1.md`).
- **Web `connectToWorker` genesis-seal "Wrong passphrase" (FIXED 2026-09-05):** the seal check built its payload by exclusion, wrongly including the `identity` object (Python/CLI seal via the ADR-029a whitelist `select_seal_fields`). `DevModeContext.jsx` now verifies canonical-first (`selectSealFields` + `jsonSort`), legacy open-set/Flutter-order formats kept as fallback. Verified live: Vivaldi restore-from-cloud → Dashboard GREEN.
- **🟠 Cross-client staging handoff (Flutter↔Web) — live E2E observed 2026-09-05, Flutter fix applied 2026-09-06:** Web receives **no auto-push** of Flutter's activity; manual "Sync Now" → `REAUTH_NEEDED` → after re-auth Web pulls+merges. **Root cause (spec violation) FIXED in Flutter:** `checkAndSync` mismatch branch (`sync_service.dart:509`) was calling `_cookie.destroyLocally(storage)` before returning `reauthNeeded` — so the next 5s tick saw "no cookie" and auto-reclaimed via `_reconcileAndClaimRowLevel()` **without consent** (violates §12.3-A1 / I1). Fix: mismatch now **preserves** the cookie (mismatch re-detected every subsequent sync → `REAUTH_NEEDED` until consent) + new `SyncService.reconcileAndClaim()` invoked by `sync_screen.dart` after explicit re-auth (mirrors CLI `_reconcile_and_claim`). Tests updated (M4/M5, G5, AS2, S3.2 + regression: 2nd `checkAndSync` stays `reauthNeeded`); full Flutter suite GREEN (2186 passed / 0 fail / 4 skip). Test artifact "StagingSessionTest" (`qvTFmSfzWz`) still in test Worker staging (cleanup pending).
- **R6 follow-up (RESOLVED 2026-08-31):** `identity_pub_key` canonical = raw-bytes (Rust `digest.rs`, PHPSPEC §2.7.1) — Web/Flutter now hash raw bytes via the `identity_pub_key` binding (was `sha256(String)` divergence). Blueprint: `docs/planning/C2_IDENTITY_PUB_KEY_RAW_BYTES_PHASE1.md`.
- **`i02a_field_token_wasm_test.mjs` — 2 formerly-failing WASM-binding tests (RESOLVED 2026-08-31):** `hmac_hex`/`derive_field_key` now exported after the WASM rebuild (`phpoc-crypto-core/pkg/*` was stale). 28/28 GREEN.
- **🔴 "activities through Aug 7 doubled" — Option (a) APPLIED to R2 (2026-08-27):** `scripts/apply_ledger_repair_r2.py` (dry-run default; `--apply` writes). Remote now VALID 141 blocks / 0 dups, `.sha256` sidecar restored. **Full restore-from-cloud per client next.** Detail: `docs/planning/AUG7_DOUBLING_REMEDIATION_PLAN.md`.
- **OPEN: Aug 13–14 2026 activities missing from remote ledger + web History** (9 committed activities in phone blocks 132–135, dropped during earlier phone repair; survive only in `/tmp/phpoc_phone_backup/pre_repair_20260814_124924/phpoc.db` staging table `committed=1`). Recovery needs re-commit from that backup or another device — not fixable in web History. Root cause (fixed `2d05aff`): `_prepareEntries` stripped `entry_id`/`hash` but retained `activity_id` → re-seeded dups.
- **🔴 Pre-existing credential leak (git history only — working tree neutralized):** personal seed/passphrase/API-key/worker-url hardcoded in `onboarding_screen.dart:205-208` + `diag_verify.dart:19` (commits `a5b124e`/`08235f8`). Working tree fixed 2026-08-21 (creds → gitignored `TEST_CREDENTIALS.md` + env var). History rewrite is user-initiated; only a C-2 seed re-key truly nullifies the leaked seed.
- **Live debug visibility:** phone `RFCW50FZQPJ` debuggable (rebuild 2026-08-19); DB dump `run-as com.phpoc.phpoc_flutter cat app_flutter/phpoc.db`. Emulator `emulator-5554` also debug.
- `_pushBlobOnly()` + `StagingPaths.remoteStagingBlob` — RETIRED ✅. `stagingStore` required/non-null.
- `verify()` after cloud restore — FIXED (Plan B: RC1–RC3, `VERIFY_RESTORE_FIX_PLAN_B.md`).

## Flutter Mobile App
- **Flutter:** 3.44.6 (stable) | **Emulator:** `pixel_6_avg` (API 35, x86_64)
- **Tech stack:** Riverpod + go_router + SQLite + SharedPreferences + flutter_secure_storage
- **Test creds:** `TEST_CREDENTIALS.md` (gitignored)
- **Cross-client sync reference:** `docs/reference/CROSS_CLIENT_STAGE_SYNCING_REFERENCE.md` §12
